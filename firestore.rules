rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write scores
    match /scores/{document} {
      allow read: if true;
      allow write: if true;
    }
  }
}
